/* Code By [ junah ]
GitHub : [ junah201 ] */

#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define all(v) (v).begin(), (v).end()

int T, N, D, K;

int solve()
{
    scanf("%d %d %d", &N, &D, &K);

        return -1;
}

int main()
{
    scanf("%d", &T);

    while (T--)
    {
        printf("%d\n", solve());
    }

    return 0;
}